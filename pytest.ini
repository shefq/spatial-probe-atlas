[pytest]
testpaths = tests
pythonpath = backend/src
addopts = -q
markers =
    api: FastAPI contract tests
    integration: SQLite, artifact and worker integration tests
    replay: deterministic hardware-free camera/tracking tests
    hardware: opt-in tests requiring physical Record3D or CUDA hardware
    slow: tests excluded from the default quick developer loop
